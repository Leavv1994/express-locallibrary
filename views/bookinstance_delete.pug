extends layout

block content
  h1 ID: #{bookinstance._id}
  p Book: #{bookinstance.book.title}, ID: #{bookinstance._id}  

    p Do you really want to delete this Bookinstance?
    
    form(method='POST' action='')
      div.form-group
        input#bookinstanceid.form-control(type='hidden',name='bookinstanceid', required='true', value=bookinstance)

      button.btn.btn-primary(type='submit') Delete Bookinstance

  